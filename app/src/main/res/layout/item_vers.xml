<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="0px"
    android:id="@+id/main_layer"
    android:orientation="vertical"
    android:background="?attr/listBackgroundColor">
    <LinearLayout
        android:id="@+id/versCardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:animateLayoutChanges="true"
        android:background="?attr/selectableItemBackground"
        android:orientation="vertical"
        android:focusable="true">
        <RelativeLayout
            android:layout_width="match_parent"
            android:background="@drawable/selector_attentions_list_item"
            android:layout_height="wrap_content"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layoutDirection="rtl"
            android:id="@+id/verse_layout">

            <TextView
                android:id="@+id/verse_text1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="2dp"
                android:paddingStart="2dp"
                android:paddingBottom="3dp"
                android:text="@string/first_verse"
                android:textColor="?attr/ListTextColor"
                android:textSize="14sp"
                android:textAlignment="gravity" />
            <TextView
                android:id="@+id/verse_text2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="2dp"
                android:paddingStart="2dp"
                android:paddingBottom="3dp"
                android:text="@string/second_verse"
                android:textColor="?attr/ListTextColor"
                android:textSize="14sp"
                android:layout_below="@id/verse_text1"
                android:textAlignment="gravity" />
        </RelativeLayout>
    </LinearLayout>
</RelativeLayout>